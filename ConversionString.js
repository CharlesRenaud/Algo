ConversionString = () => {
    var words = ["the", "quick", "brown", "fox"];
    var wordLengths = words.map(word => word.length);
    console.log(wordLengths)
    // wordLengths = [3, 5, 5, 3]
}
ConversionString();